create table vehicle(
price decimal(8,2),
mileage mediumint(8) unsigned,
make varchar(20),
model varchar(20),
trim varchar(20),
type varchar(20),
cylinders tinyint(3) unsigned,
liters decimal(3,1),
doors tinyint(3) unsigned,
cruise bit(1),
sound bit(1),
leather bit(1)
)